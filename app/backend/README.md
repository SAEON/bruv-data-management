# Backend reamdme

**FastAPI Setup**

Create virtual environment and activate it for backend

`cd app/backend`
`python -m venv venv`
`source venv/bin/activate`

**Install requirements**

`pip install -r requirements.txt`


**To run backend**

`uvicorn main:app --reload`



